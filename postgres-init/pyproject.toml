[project]
name = "postgres-init"
version = "0.1.0"
description = "PostgreSQL idempotent initialization"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"